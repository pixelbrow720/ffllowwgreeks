# Rev 12 SRE-16 — these are the *direct* runtime dependencies. The
# tool-generated lockfile that resolves transitive deps + per-wheel SHA-256
# hashes lives next to this file as ``requirements.lock.txt`` and is what
# the Dockerfile installs with ``pip install --require-hashes -r ...`` for
# supply-chain integrity. See OPS.md §7b for the pip-compile workflow.
#
# TODO Rev12: generate ``requirements.lock.txt`` via
#   ``pip-compile --generate-hashes --output-file requirements.lock.txt requirements.txt``
# (run from a clean virtualenv with pip-tools installed). The Dockerfile
# falls back to this unpinned file when the lockfile is absent.
fastapi==0.115.4
uvicorn[standard]==0.32.0
pydantic==2.9.2
pydantic-settings==2.6.1
SQLAlchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg[binary]==3.2.3
alembic==1.13.3
databento==0.47.0
pandas==2.2.3
numpy==2.1.3
scipy==1.14.1
APScheduler==3.10.4
PyJWT==2.9.0
bcrypt==4.2.0
passlib[bcrypt]==1.7.4
cryptography==43.0.3
python-multipart==0.0.17
slowapi==0.1.9
structlog==24.4.0
holidays==0.58
httpx==0.27.2
pytest==8.3.3
pytest-asyncio==1.0.0
pytest-cov==5.0.0
hypothesis==6.115.5
ruff==0.7.2
